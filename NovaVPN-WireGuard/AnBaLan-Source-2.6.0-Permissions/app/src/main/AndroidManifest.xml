<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.anbalana"> <!-- ВАЖНО: Замени на имя своего пакета! -->

    <!-- Базовые сетевые разрешения для работы с сокетами и трафиком -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Разрешения для работы сервиса в фоне (обязательно для Android 9 и выше) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    
    <!-- Разрешение на уведомления (обязательно для Android 13+ чтобы сервис не убила система) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="AnBaLana FakeLag"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
        android:usesCleartextTraffic="true">

        <!-- Главное окно приложения (UI) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- КРИТИЧЕСКИ ВАЖНО: Регистрация VPN-сервиса -->
        <!-- Атрибут android:permission="android.permission.BIND_VPN_SERVICE" гарантирует, -->
        <!-- что только система Android может запускать и управлять этим сервисом. -->
        <service
            android:name=".MyVpnService"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:exported="true"
            android:foregroundServiceType="specialUse">
            <intent-filter>
                <!-- Этот action говорит системе, что сервис является обработчиком VPN -->
                <action android:name="android.net.VpnService" />
            </intent-filter>
        </service>

    </application>

</manifest>